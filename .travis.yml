language: node_js

node_js:
   - "0.8"

before_script:
  - npm -q install
  - npm -q install -g testem
  - npm -q install -g bower
  - bower install -q
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
script:
  - ./testem -l Firefox,PhantomJS
